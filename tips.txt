
git clone https://github.com/rotemelz/coursera-test.git

One time:
git config --global credential.helper store

git config user.name rotemelz
git config user.email "rotem.elzin@gmail.com"

git commit -a -m "try 4"
git status
git push

browser-sync start --server --directory --files "*"